{
	"name": "buntodo",
	"prettier": "@workspace/prettier-config",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"test": "lerna run test",
		"format": "lerna run format",
		"format:fix": "lerna run format:fix"
	},
	"dependencies": {},
	"devDependencies": {
		"lerna": "^7.0.2"
	}
}
