{
	"name": "buntodo",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "lerna run dev",
		"test": "lerna run test",
		"format": "lerna run format",
		"format:fix": "lerna run format:fix",
		"prepare": "husky install"
	},
	"devDependencies": {
		"husky": "^8.0.0",
		"lerna": "^7.0.2",
		"lint-staged": "^13.2.2"
	},
	"lint-staged": {
		"*.{ts,json}": [
			"prettier --write"
		]
	}
}
